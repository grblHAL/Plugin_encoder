## Encoder plugin

This plugin can be used for quadrature encoder input to adjust overrides.

One of two experimental jogging modes can be enabled at compile time, absolute or relative.

Dependencies:

Driver must have low level support for at least one encoder. Up to five is supported.

---
2025-12-23
